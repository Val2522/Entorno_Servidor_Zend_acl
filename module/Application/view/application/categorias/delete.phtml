<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Eliminar Categoría</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">🎮 Retro Games</a>
            <a href="/categorias" class="btn btn-light btn-sm ms-auto">← Volver</a>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="card border-danger">
                    <div class="card-header bg-danger text-white">
                        <h5 class="card-title mb-0">⚠️ Confirmar Eliminación</h5>
                    </div>
                    <div class="card-body">
                        <h4 class="mb-3">¿Deseas eliminar esta categoría?</h4>
                        
                        <div class="alert alert-warning">
                            <strong>Categoría:</strong> <?php echo htmlspecialchars($this->categoria['nombre']); ?>
                        </div>

                        <form method="post" class="d-flex gap-2">
                            <button type="submit" name="si" class="btn btn-danger btn-lg flex-grow-1">
                                ✓ Sí, eliminar
                            </button>
                            <button type="submit" name="no" class="btn btn-secondary btn-lg flex-grow-1">
                                ✗ Cancelar
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
