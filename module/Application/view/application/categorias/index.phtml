<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Categorías - Administración</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">🎮 Retro Games</a>
            <a href="/" class="btn btn-light btn-sm ms-auto">← Volver</a>
        </div>
    </nav>

    <div class="container mt-4">
        <h1>📂 Administración de Categorías</h1>
        <a href="/categorias/new" class="btn btn-primary mb-3">+ Nueva Categoría</a>
        
        <?php if ($this->categorias): ?>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead class="table-dark">
                        <tr>
                            <th>ID</th>
                            <th>Nombre</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($this->categorias as $cat): ?>
                            <tr>
                                <td><?php echo $cat['id']; ?></td>
                                <td><?php echo htmlspecialchars($cat['nombre']); ?></td>
                                <td>
                                    <a href="/categorias/<?php echo $cat['id']; ?>/edit" class="btn btn-sm btn-warning">Editar</a>
                                    <a href="/categorias/<?php echo $cat['id']; ?>/delete" class="btn btn-sm btn-danger">Eliminar</a>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        <?php else: ?>
            <div class="alert alert-info">No hay categorías.</div>
        <?php endif; ?>
    </div>

    <footer class="bg-dark text-white text-center py-4 mt-5">
        <p class="mb-0">&copy; 2024 Tienda de Videojuegos Retro</p>
    </footer>
</body>
</html>
