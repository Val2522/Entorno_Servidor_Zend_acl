<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nueva Categoría</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">🎮 Retro Games</a>
            <a href="/categorias" class="btn btn-light btn-sm ms-auto">← Volver</a>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="card">
                    <div class="card-body">
                        <h1 class="card-title mb-4">
                            <?php echo isset($this->categoria) ? 'Editar Categoría' : 'Nueva Categoría'; ?>
                        </h1>
                        
                        <form method="post">
                            <div class="mb-3">
                                <label for="nombre" class="form-label">Nombre de la categoría:</label>
                                <input type="text" class="form-control" id="nombre" name="nombre" 
                                       value="<?php echo isset($this->categoria) ? htmlspecialchars($this->categoria['nombre']) : ''; ?>"
                                       required>
                            </div>

                            <button type="submit" class="btn btn-primary btn-lg w-100">
                                Guardar
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
