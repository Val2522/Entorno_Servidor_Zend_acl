<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agregar al Carrito - Tienda de Videojuegos Retro</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">🎮 Retro Games</a>
            <div class="ms-auto">
                <a href="/" class="btn btn-light btn-sm">← Volver</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="card">
                    <div class="card-body">
                        <h1 class="card-title mb-4">
                            Agregar a carrito
                        </h1>
                        
                        <?php if ($this->articulo): ?>
                            <div class="mb-4">
                                <img src="/uploads/<?php echo htmlspecialchars($this->articulo['image'] ?? 'not-found.png'); ?>" 
                                     class="img-fluid rounded" alt="<?php echo htmlspecialchars($this->articulo['nombre']); ?>">
                            </div>

                            <h2><?php echo htmlspecialchars($this->articulo['nombre']); ?></h2>
                            <p class="text-muted"><?php echo htmlspecialchars($this->articulo['descripcion']); ?></p>

                            <div class="alert alert-info">
                                <strong>Precio:</strong> 
                                <?php 
                                $precioFinal = $this->articulo['precio'] + ($this->articulo['precio'] * $this->articulo['iva'] / 100);
                                echo number_format($precioFinal, 2, ',', '.');
                                ?>€ (IVA: <?php echo $this->articulo['iva']; ?>%)
                                <br>
                                <strong>Stock disponible:</strong> <?php echo $this->articulo['stock']; ?>
                            </div>

                            <form method="post">
                                <div class="mb-3">
                                    <label for="cantidad" class="form-label">
                                        <strong>¿Cuántos quieres?</strong>
                                    </label>
                                    <input type="number" name="cantidad" id="cantidad" class="form-control" 
                                           min="1" max="<?php echo $this->articulo['stock']; ?>" value="1" required>
                                </div>

                                <button type="submit" class="btn btn-success btn-lg w-100">
                                    ✓ Agregar al carrito
                                </button>
                            </form>
                        <?php else: ?>
                            <div class="alert alert-danger" role="alert">
                                El videojuego no fue encontrado.
                            </div>
                        <?php endif; ?>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer class="bg-dark text-white text-center py-4 mt-5">
        <p class="mb-0">&copy; 2024 Tienda de Videojuegos Retro</p>
    </footer>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
