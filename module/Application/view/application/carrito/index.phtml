<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tu Carrito - Tienda de Videojuegos Retro</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">🎮 Retro Games</a>
            <div class="ms-auto">
                <a href="/" class="btn btn-light btn-sm">← Volver</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <h1>🛒 Tu Carrito de Compra</h1>
        
        <?php if ($this->articulos && count($this->articulos) > 0): ?>
            <div class="table-responsive mt-4">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Videojuego</th>
                            <th>Precio</th>
                            <th>Cantidad</th>
                            <th>Subtotal</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php 
                        $count = 0;
                        foreach ($this->articulos as $articulo): 
                            $cantidad = $this->cantidades[$count];
                            $precioFinal = $articulo['precio'] + ($articulo['precio'] * $articulo['iva'] / 100);
                            $subtotal = $precioFinal * $cantidad;
                        ?>
                            <tr>
                                <td><?php echo htmlspecialchars($articulo['nombre']); ?></td>
                                <td><?php echo number_format($precioFinal, 2, ',', '.'); ?>€</td>
                                <td><?php echo $cantidad; ?></td>
                                <td><?php echo number_format($subtotal, 2, ',', '.'); ?>€</td>
                                <td>
                                    <a href="/carrito/delete/<?php echo $articulo['id']; ?>" 
                                       class="btn btn-sm btn-danger" onclick="return confirm('¿Eliminar del carrito?')">
                                        Eliminar
                                    </a>
                                </td>
                            </tr>
                        <?php $count++; endforeach; ?>
                    </tbody>
                </table>
            </div>

            <div class="row mt-4">
                <div class="col-md-6 offset-md-6">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">Resumen del pedido</h5>
                            <hr>
                            <div class="d-flex justify-content-between mb-3">
                                <strong>Total:</strong>
                                <strong class="text-success" style="font-size: 1.3rem;">
                                    <?php echo number_format($this->total, 2, ',', '.'); ?>€
                                </strong>
                            </div>
                            <a href="/pedido" class="btn btn-success btn-lg w-100">
                                ✓ Realizar Pedido
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        <?php else: ?>
            <div class="alert alert-info mt-4" role="alert">
                <h4 class="alert-heading">Tu carrito está vacío</h4>
                <p>Aún no has agregado ningún videojuego a tu carrito.</p>
                <a href="/" class="btn btn-primary">Ir a comprar</a>
            </div>
        <?php endif; ?>
    </div>

    <footer class="bg-dark text-white text-center py-4 mt-5">
        <p class="mb-0">&copy; 2024 Tienda de Videojuegos Retro</p>
    </footer>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
